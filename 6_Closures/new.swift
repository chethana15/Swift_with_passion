import Foundation

var T = 4
while(T != 0){
   
//    let values = readLine()?.components(separatedBy: CharacterSet.whitespacesAndNewlines).compactMap { Int($0) }
    let X = 5
    let Y = 3
    // var steps = Int(X/Y)
    // if((steps*Y) != X)
    //  {
    //     steps = steps + (X - (steps*Y))
    //  }
     print(X/Y+X%Y)
    //  steps = 0
     T = T - 1
}